[project]
name = "language-steering"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "pandas>=2.3.3",
    "steering-vectors>=0.12",
    "torch>=2.0",
    "transformers>=4.40",
    "accelerate>=0.30",
]
